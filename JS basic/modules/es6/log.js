const log = data => {
	console.log(data);
};

//  module.exports = log
export default log;

//  Note -> 1. only 1 default export is allowed
//  only default export can be renamed
// for named exports we use alias

// //////////////////////////////////////
//  full stack MERN
//  server side code (only in js) -> node common.js modules
// client side  -> es6 modules
//  typescript /babel tool ->  es6 modules
