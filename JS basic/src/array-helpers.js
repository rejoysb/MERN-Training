const arr = [1, 2, 3, 4];
// arr.forEach((el, i) => console.log(el, i));
// arr.map(el => el * 2);   // -> new mapped array
// arr.find((el)=>el==2)  - // ->undefined, value
// arr.findIndex((el)=>el==2) // ->index, -1
